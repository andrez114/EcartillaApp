<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.millan.ecartillav112">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecartillav112">
        <activity android:name=".DescriptionArtActivity"
            android:parentActivityName=".MenuActivity"></activity>
        <activity android:name=".InfoPerfil" />
        <activity android:name=".DescriptionActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".RecuperarContra" />
        <activity android:name=".HomeActiviy" />
        <activity android:name=".NewUserActivity" />
        <activity android:name=".loginActivity" />
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>